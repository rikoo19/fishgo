Authentication removed

The authentication package was removed from the project per recent scope changes.

If you need to restore authentication later, reintroduce the `backend/auth` modules and include the router in `backend/main.py`.
